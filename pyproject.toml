[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "Reusable maze generator module"
authors = [
    { name = "masselgu & selhor" }
]
readme = "README.md"
requires-python = ">=3.10"

dependencies = []