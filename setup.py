from setuptools import setup, find_packages

setup(
    name="mazegen",
    version="1.0.0",
    packages=find_packages(),
    description="Maze generator and solver",
    author="massegu & selhor",
    python_requires=">=3.8",
)
